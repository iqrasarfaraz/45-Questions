import { mainModule } from "process";

//solution
function makeShirt(size:string,message:string){
    console.log(`A ${size}- sized shirt will e printed with the message: "${message}".`);
    
}
//call the function
makeShirt("large", "iqra rao");